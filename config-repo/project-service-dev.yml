spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/projectdb_dev
    username: postgres
    password: {cipher}encrypted-dev-pass
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
server:
  port: 8082
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
springdoc:
  swagger-ui:
    path: /swagger-ui